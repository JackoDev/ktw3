<%= form_with(model: current_user.active_followships.find_by(followee_id: @user.id),
  html: { method: :delete }, local: true) do |f| %>
      <%= f.submit "Unfollow", class: "btn btn-outline-warning btn-lg" %>
  <% end %>